image: python:3.7

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

cache:
  paths:
    - .cache/pip
    - .tox

before_script:
    - pip install tox==3.13.2

py37:
  image: python:3.7
  script:
    - tox
